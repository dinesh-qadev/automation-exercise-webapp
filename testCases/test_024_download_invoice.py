'''
@allure.feature("Orders")
@allure.severity(allure.severity_level.NORMAL)
@allure.title("Download Invoice After Purchase")
@allure.description("Verify invoice can be downloaded after successful order placement.")
'''